---
apiVersion: v1
kind: Namespace
metadata:
  name: app
  labels:
    istio-injection: enabled

---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: app-v1
  namespace: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-v1
  template:
    metadata:
      labels:
        app: app-v1
    spec:
      containers:
      - name: app-v1
        image: containous/traefikee-webapp-demo:v2
        imagePullPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: app-v1
  namespace: app
  labels:
    app: app-v1
  annotations:
    traefik.ingress.kubernetes.io/service.nativelb: "true"
spec:
  type: ClusterIP
  ports:
  - port: 80
    name: app-v1
  selector:
    app: app-v1

---
apiVersion: v1
kind: Service
metadata:
  name: app-external
  namespace: app
  annotations:
    traefik.ingress.kubernetes.io/service.nativelb: "true"
spec:
  type: ExternalName
  externalName: app-v1.app.svc.cluster.local

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: app-tls
  namespace: app
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`app.wmtls.demo.traefiklabs.tech`)
      services:
        - name: app-external
          port: 80
          passHostHeader: false
